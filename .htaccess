RewriteEngine On

# Redireciona tudo para index.php se não for arquivo ou diretório existente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]

# Permite URLs amigáveis sem .php
AddDefaultCharset utf-8
